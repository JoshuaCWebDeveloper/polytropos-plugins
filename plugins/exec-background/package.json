{
  "name": "@polytropos/exec-background",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json && node ../../scripts/pack-plugin.mjs exec-background",
    "test": "rimraf dist-test && tsc -p tsconfig.test.json && node --test dist-test/*.test.js"
  },
  "dependencies": {
    "openclaw": "*"
  }
}
