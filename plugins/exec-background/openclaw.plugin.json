{
  "id": "exec-background",
  "name": "Exec Background",
  "version": "0.1.0",
  "description": "Adds detached background execution to the outer exec_command adapter without replacing OpenClaw exec's native managed sessions.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "logDirectory": {
        "type": "string",
        "minLength": 1,
        "description": "Directory for per-launch stdout/stderr logs. Defaults to /tmp/openclaw-exec-background."
      }
    }
  },
  "entry": "dist/index.js"
}
