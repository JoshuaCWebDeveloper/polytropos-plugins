{
  "id": "channel-context-overlay",
  "name": "Channel Context Overlay",
  "description": "Inject per-channel system context (e.g. Software Engineering Mode operating contract) from live-editable overlay files.",
  "version": "0.2.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "overlaysDir": {
        "type": "string",
        "description": "Directory containing overlay files named channel-<id>.md"
      }
    }
  },
  "entry": "dist/index.js"
}
