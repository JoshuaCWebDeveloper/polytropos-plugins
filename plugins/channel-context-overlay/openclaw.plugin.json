{
  "id": "channel-context-overlay",
  "name": "Channel Context Overlay",
  "description": "Inject per-channel system context (e.g. Software Engineering Mode operating contract) from live-editable per-channel overlay directories.",
  "version": "0.2.1",
  "contracts": {
    "tools": ["channel_context_overlay"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "overlaysDir": {
        "type": "string",
        "description": "Directory containing per-channel overlay directories named channel-<id>"
      }
    }
  },
  "entry": "dist/index.js"
}
